# DualLED Pro — runtime dependencies
#
# None. The app has zero third-party runtime dependencies — it needs only
# Python 3.8+. Controller I/O talks to the native Windows HID stack directly
# via ctypes (see ps5led/hid_win.py); the browser-based UI is plain ES
# modules and vendored three.js, with no bundler and no build step.
